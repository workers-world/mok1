{
    "name": "mok1",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "check": "npm run lint && tsc --noEmit",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage",
        "lint": "npx --yes @biomejs/biome@2.5.1 check src",
        "format": "npx --yes @biomejs/biome@2.5.1 format --write src"
    },
    "dependencies": {
        "hono": "^4"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "4.20260623.1",
        "@vitest/coverage-v8": "^3.2.7",
        "typescript": "^5.9.3",
        "vitest": "^3.2.7",
        "wrangler": "4.103.0"
    }
}
